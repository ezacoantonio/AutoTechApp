{
  "name": "mechanic-mindset",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm.cmd install --prefix server && npm.cmd install --prefix client && npm.cmd install",
    "dev": "concurrently \"npm.cmd run dev --prefix server\" \"npm.cmd run dev --prefix client\"",
    "dev:server": "npm.cmd run dev --prefix server",
    "dev:client": "npm.cmd run dev --prefix client",
    "build": "npm.cmd run build --prefix client",
    "seed": "npm.cmd run seed --prefix server",
    "start": "npm.cmd run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

